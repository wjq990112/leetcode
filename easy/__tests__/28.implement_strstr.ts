/**
 * @file 28. 实现 strStr()
 */
import strStr from '../28.implement_strstr';

describe('28. 实现 strStr()', () => {
  it('input: haystack = "hello", needle = "ll"', () => {
    expect(strStr('hello', 'll')).toEqual(2);
  });

  it('input: haystack = "aaaaa", needle = "bba"', () => {

  });
});
